# Sfinity

Monorepo đồ án: mobile (Flutter) + web-admin + backend.

```
Sfinity/
├── mobile/       # App end-user — Android & iOS
├── web-admin/    # Trang quản trị (browser)
└── backend/      # API chung
```

Chạy mobile: `cd mobile` → `flutter run`
